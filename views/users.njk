{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
    <h1>All users</h1>

    <ul>
        {% for user in users %}
        <li><a href="/users/{{user.id}}" class="button">{{ user.username }}</a></li>
        {% endfor %}
    </ul>
</main>
{% endblock %}